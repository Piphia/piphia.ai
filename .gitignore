# Dependencies
/node_modules

# Production build
/build

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# screenshot source originals — site ships the optimized webp in static/img/screenshots
/screenshots/

# brand exploration / logo design scratch — NOT referenced by the site (the favicon
# and served logos live in static/img/). Docusaurus copies everything under static/
# to the deploy root, so keep this design dump out of both the public repo and the
# published site.
/static/brand/
